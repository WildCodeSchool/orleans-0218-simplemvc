{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Add a new item</h1>

    <ul class="alert alert-danger">
        {% for error in errors %}
            <li>{{ error }}</li>
        {% endfor %}
    </ul>

    {% include 'Item/_form.html.twig' %}
{% endblock %}
